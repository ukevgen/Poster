<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="15dp"
    android:paddingRight="15dp"
    >

    <com.agilie.poster.view.custom.SquareImageView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/settings_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:adjustViewBounds="true"

        android:scaleType="centerCrop"
        />

    <android.support.v7.widget.AppCompatTextView
        android:id="@+id/settings_text"
        style="@style/ImageSettingText"
        android:paddingTop="2dp"
        app:layout_constraintLeft_toLeftOf="@+id/settings_image"
        app:layout_constraintRight_toRightOf="@+id/settings_image"
        app:layout_constraintTop_toBottomOf="@+id/settings_image"/>

</android.support.constraint.ConstraintLayout>